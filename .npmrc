# Actual pnpm supply-chain settings (minimumReleaseAge, allowBuilds) live in
# pnpm-workspace.yaml for this pnpm version — see the comment there. This
# file is currently just a placeholder for any future npm/pnpm registry
# settings that do belong here (e.g. a private registry mapping).
